# At the top of your file add this function
def load_svg(path):
    with open(path, "r") as f:
        return f.read()

# Then in your home page section replace st.title with:
svg = load_svg("logo.svg")
st.markdown(svg, unsafe_allow_html=True)